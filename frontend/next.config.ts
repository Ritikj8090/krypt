import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
    domains: ['www.omnisend.com', "i.pinimg.com", "acegif.com", "media4.popsugar-assets.com", "metro.co.uk", "api.giphy.com"]
  }
};

export default nextConfig;
